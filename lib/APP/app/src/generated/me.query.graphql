query MeQuery {
    me {
        id
        email
        first_name
        last_name
        date_of_birth
        address
        city {
        id
        name
        }
        postal
        stripe_account_id
        stripe_customer_id
    }
}
