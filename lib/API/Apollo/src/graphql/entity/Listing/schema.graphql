type Listing {
    posted: String!
    
}

type Query {
    getListing(id: String!): Listing
}
type Mutation {
    addListing(name: String!): Listing!
}

type Subscription {
    watchListing(id: String!): Listing!
}